# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- Master

pool:
  vmImage: ubuntu-latest

jobs:
 - job: FirstJob
   timeoutInMinutes: 10 
   steps:
   - bash: echo hellow dear 

 - job: SecondJob
   steps:
     - bash: echo this hello by the second step  
